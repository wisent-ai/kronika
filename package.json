{
  "name": "@wisent-ai/docs-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generate product documentation from repository truth through a typed, validated content plan.",
  "bin": { "docs-cli": "./src/run.mjs" },
  "scripts": {
    "detect": "node src/detect.mjs",
    "validate": "node src/validate.mjs",
    "emit": "node src/emit.mjs"
  },
  "engines": { "node": ">=20" },
  "license": "UNLICENSED"
}
